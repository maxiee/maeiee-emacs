#+TITLE: 41 Orderless：用多个片段描述目标
#+PROPERTY: header-args:emacs-lisp :results silent :comments link
#+STARTUP: overview

* 本章目标

允许以空格分隔多个匹配片段，不要求它们连续出现或顺序固定。

* 需要理解的概念

- 补全由“候选集合”和“匹配策略”组成。
- Orderless 是匹配策略；它与 Vertico、Consult 等前端可以自由组合。

* 配置

#+begin_src emacs-lisp
(use-package orderless
  :demand t
  :custom
  (completion-styles '(orderless basic))
  (completion-category-defaults nil)
  (completion-category-overrides
   '((file (styles partial-completion)))))
#+end_src

* 动手验证

执行 =M-x=，输入类似 =buff kill=，观察它是否匹配 =kill-current-buffer= 等候选。

* 维护笔记

- 修改后保存，本仓库会自动 tangle 当前模块。
- 想立即应用本章，执行 =M-x maeiee-reload-current-module=。
- 生成的 =41-orderless.el= 位于 =generated/=，不要直接编辑。
